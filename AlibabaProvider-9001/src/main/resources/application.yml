server:
  port: 9001

spring:
  application:
    name: Alibaba-Payment-Provider
  cloud:
    nacos:
      discovery:
        server-addr: Cying.com:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"