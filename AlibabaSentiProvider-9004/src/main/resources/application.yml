server:
  port: 9004

spring:
  application:
    name: Sentinel-Provider
  cloud:
    nacos:
      discovery:
        server-addr: cying.com:8848

management:
  endpoints:
    web:
      exposure:
        include: "*"
